# ===========================================
# CẤU HÌNH ỨNG DỤNG CHO DOCKER
# ===========================================
NODE_ENV=production
APP_NAME=ReadingBook
APP_HOST=0.0.0.0
APP_PORT=9000
API_VERSION=v1
API_PREFIX=/api

# ===========================================
# CẤU HÌNH FIREBASE (BẮT BUỘC)
# ===========================================
# Lấy từ Firebase Console > Project Settings > General
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_PROJECT_NUMBER=your-project-number
FIREBASE_DATABASE_URL=https://your-project.firebaseio.com
# Lấy từ Firebase Console > Project Settings > General > Web API Key
FIREBASE_WEB_API_KEY=your-web-api-key

# ===========================================
# CẤU HÌNH EMAIL (BẮT BUỘC)
# ===========================================
# Cấu hình SMTP cho Gmail
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
EMAIL_FROM=your-email@gmail.com

# ===========================================
# CẤU HÌNH TWILIO (CHO SMS - TÙY CHỌN)
# ===========================================
# Lấy từ Twilio Console > Account > API keys & tokens
TWILIO_ACCOUNT_SID=your-account-sid
TWILIO_AUTH_TOKEN=your-auth-token
TWILIO_PHONE_NUMBER=your-twilio-number

# ===========================================
# BẢO MẬT VÀ GIỚI HẠN TỐC ĐỘ
# ===========================================
# Giới hạn số request trong 1 khoảng thời gian
RATE_LIMIT=100
RATE_LIMIT_WINDOW=15

# Cấu hình CORS cho Docker
CORS_ORIGIN=*
CORS_METHODS=GET,POST,PUT,DELETE,PATCH
CORS_CREDENTIALS=true

# ===========================================
# CẤU HÌNH UPLOAD
# ===========================================
# Giới hạn kích thước file upload
UPLOAD_LIMIT=5mb
# Các định dạng file được phép
ALLOWED_FORMATS=jpg,jpeg,png,pdf,epub
# Thư mục lưu trữ file upload
STORAGE_PATH=uploads/

# ===========================================
# CẤU HÌNH OTP
# ===========================================
# Độ dài mã OTP
OTP_LENGTH=6
# Thời gian hết hạn OTP (giây)
OTP_EXPIRY=300
# Phương thức gửi OTP (email hoặc sms)
OTP_PROVIDER=email

# ===========================================
# CẤU HÌNH CACHE
# ===========================================
# Thời gian sống của cache (giây)
CACHE_TTL=300
# Thời gian kiểm tra cache (giây)
CACHE_CHECKPERIOD=120

# ===========================================
# CẤU HÌNH JWT (BẮT BUỘC)
# ===========================================
# Secret key để ký JWT token (nên dùng chuỗi ngẫu nhiên mạnh)
JWT_SECRET=your-super-secret-jwt-key-here
# Thời gian hết hạn của access token
JWT_EXPIRY=1h

# ===========================================
# CẤU HÌNH LOGGING
# ===========================================
# Mức độ log (error, warn, info, debug)
LOG_LEVEL=info
# Định dạng log
LOG_FORMAT=combined

# ===========================================
# CẤU HÌNH REDIS (CHO DOCKER)
# ===========================================
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=

# ===========================================
# HƯỚNG DẪN SỬ DỤNG
# ===========================================
# 1. Copy file này thành .env: cp env.docker.example .env
# 2. Thay thế tất cả giá trị "your-*" bằng giá trị thực tế
# 3. Đảm bảo file .env không được commit vào Git
# 4. Đặt file serviceAccountKey.json vào thư mục gốc
# 5. Chạy: docker-compose up --build
